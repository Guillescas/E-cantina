import { ReactElement } from 'react';

import { StylesContainer } from './styles';

const RestaurantListDashboard = (): ReactElement => {
  return (
    <StylesContainer>
      <h1>opa</h1>
    </StylesContainer>
  );
};

export default RestaurantListDashboard;
