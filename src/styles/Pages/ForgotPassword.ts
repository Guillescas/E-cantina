import styled from 'styled-components';

export const StylesContainer = styled.div`
  max-width: 1200px;
  margin: 0 auto;

  form {
    max-width: 768px;
    margin: 0 auto;

    margin-top: 4rem;
  }
`;
